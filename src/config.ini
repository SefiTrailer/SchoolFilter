; ==============================================================================
; SchoolFilter Configuration File
; ==============================================================================
; Student computers will dynamically fetch the whitelist from this GitHub Raw URL.
; To update the whitelist for all computers in the lab:
; Simply edit 'src/filter.pac' on GitHub (https://github.com/SefiTrailer/SchoolFilter).
; All lab computers will automatically apply the changes without reinstallation!
; ==============================================================================
CloudPacUrl=https://raw.githubusercontent.com/SefiTrailer/SchoolFilter/main/src/filter.pac
